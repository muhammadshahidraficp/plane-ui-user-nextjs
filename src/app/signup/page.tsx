import { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'Sign up',
}

export default function Signup() {

    return (
        <div className="bg-full bg-blue-50 min-h-screen text-blue-500">
            Signup
        </div>
    );
}
